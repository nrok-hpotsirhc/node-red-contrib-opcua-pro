# PKI directory — auto-generated certificates

# Own certificate and private key are generated on first start.
# NEVER commit private keys or generated certificates to version control.
own/
trusted/
rejected/
issuers/
